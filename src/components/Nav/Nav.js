import React from 'react'
import Button from '../Button/Button'

export default function Nav() {
  return (
    <nav>
        <Button txt = "Add new Folder" />
        <Button txt = "Add new File" />
    </nav>
  )
}
